server:
  servlet:
    context-path: /api/v1
spring:
  application:
    name: spring-boot-azure-webapp

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always